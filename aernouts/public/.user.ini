; Testomgeving: OPcache moet bestandswijzigingen oppikken na een deploy.
; Combell draait standaard met validate_timestamps=0 (snel, maar nieuwe code
; wordt dan niet geladen). Hier forceren we revalidatie per request.
opcache.validate_timestamps=1
opcache.revalidate_freq=0
